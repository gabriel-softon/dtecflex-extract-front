import { Routes } from "@angular/router";
import { InicioComponent } from "./inicio.component";

export const INICIO_ROUTING: Routes = [
    {
        path: '', component: InicioComponent,
        providers: []
    }
]